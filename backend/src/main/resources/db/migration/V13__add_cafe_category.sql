-- Add CAFE category support
-- The places.category column uses VARCHAR, so no schema change is needed.
-- This migration exists to document the addition of CAFE as a valid category in the application.
SELECT 1;
