After frontend changes, validate with at least npm run build from the frontend directory. Prefer also running npm run lint and relevant Jest/Playwright tests when practical. For i18n changes, update both src/messages/en.json and src/messages/ms.json. For map/location features, follow the existing dynamic Leaflet import pattern and ensure client-only localStorage usage stays inside effects or user actions.