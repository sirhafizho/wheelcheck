Frontend code uses TypeScript function components with App Router client pages marked 'use client' when needed. Keep interfaces/types at module scope (important for Turbopack). Reuse next-intl translations from src/messages/*.json. UI styling uses Tailwind utility classes with accessible min-height/touch targets. Existing patterns favor hooks for data fetching, dynamic imports for Leaflet map components with ssr:false, and localStorage keys wheelcheck_token and wheelcheck_user_id for auth context.